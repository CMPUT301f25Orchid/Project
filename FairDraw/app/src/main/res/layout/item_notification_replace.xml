<?xml version="1.0" encoding="utf-8"?>
<!-- Organizer: rejected invite -> 'Replace' action (lost bg + red pill) -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/cardReplace"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:foreground="?attr/selectableItemBackground"
    android:background="@drawable/notification_card_lost">

    <!-- Row: icon | message | Replace pill -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="14dp"
        android:gravity="center_vertical">

        <!-- Person icon (swap with vector later if you want) -->
        <TextView
            android:id="@+id/tvIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="👤"
            android:textSize="18sp"
            android:layout_marginEnd="10dp"
            android:contentDescription="User icon"/>

        <!-- Message text -->
        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="User 105 has rejected invite for event “Swimming Lessons for Beginners”"
            android:textColor="#202124"
            android:textSize="16sp"/>

        <!-- Replace pill (red) -->
        <TextView
            android:id="@+id/btnReplace"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Replace"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:background="@drawable/cta_pill_red"
            android:paddingLeft="12dp"
            android:paddingRight="12dp"
            android:contentDescription="Replace selected user"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
